export interface PortfolioHistory {
    id?: number;
    dateTime: string;          // e.g. "2024-12-20"
    totalValue: number;     // e.g. 105000
    // portfolioId? etc. if needed for the client
  }
  